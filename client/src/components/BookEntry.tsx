import React from "react";


export interface BookEntryProps {
    title: string,
    description: string,
    
};

export const BookEntry: React.FC<BookEntryProps> = ({}) => {
    
    return (
        <>
        </>
    )
}